namespace IntrusionDetectionSystem;

public class Statistics
{
    //retrieve data from db
    
    //calculate mean, median and mode
    // calculate variance and standard deviation
    // calculate variation coefficient
    
    //variance = (1/n - 1) sum(i= 1, -->n)(xi - avg(x))
    //st.dev = sq.root(variance)
    
    //Method to request averages from DB with given time windows
    public static bool isDeviating(List<long> values, List<int> count)
    {
        bool isDeviating = false;
        
        
        return isDeviating;
    }

    private static double ComputeMean()
    {
        double mock = 3.2;
        return mock;
    }

    private static double ComputeMedian()
    {
        double mock = 3.2;
        return mock;
    }
    private static double ComputeMode()
    {
        double mock = 3.2;
        return mock;
    }
    
    private static double ComputeVariance()
    {
        double mock = 3.2;
        return mock;
    }
    
    private static double ComputeStandardDev()
    {
        double mock = 3.2;
        return mock;
    }
    
    private static double ComputeVarianceCoeff()
    {
        double mock = 3.2;
        return mock;
    }
        
}